language: python
env:
  global: 
    - TEST_DIR="$PWD/core/test"
    - HOME_DIR="$PWD"
before-script:
  - pip install -r requirements.txt
script: 
  - cd $TEST_DIR && ls &&python -m unittest discover
  - cd $HOME_DIR && pyinstaller --onefile  --name octane  main.py
after-script:
  - echo "publish"
